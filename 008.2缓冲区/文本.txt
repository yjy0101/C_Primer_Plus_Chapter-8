//用户输入字符后立即重复打印该字符是属于 无缓冲（或直接）输入，即正在等待的程序可立即使用输入的字符。
//对于echo.c中的例子，在用户按下Enter之前都不会重复打印刚输入的字符，这种输入形式属于 缓冲输入。
//用户输入的字符被收集并储存在一个被称为 缓冲区 （buffer）的临时储存区，按下Enter后，程序才可使用用户输入的字符。

好处：可以把若干字符作为一个块进行传输比逐个发送这些字符 节约时间
	  还可以直接通过键盘修正错误

缓冲分为两类：完全缓冲I/O 和 行缓冲I/O